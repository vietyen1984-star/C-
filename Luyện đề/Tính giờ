#include <bits/stdc++.h>
using namespace std;

int main() 
{
    int a,b;
    cin>>a>>b;
    int ans=0;
    if(a<9){
        ans=9*60-a*60-b;
    }
    if(a>9){
        ans=9*60+12*60-a*60-b;
    }
    if(a==9){
        if(b>0) cout<<b;
        else {cout<<0;
        return 0; }
    }
    cout<<ans;
    return 0;
}

//Nhập vào dãy n số. Hãy in ra số lớn nhất, bé nhất của dãy.
#include <iostream>
using namespace std;

int main() 
{
    int n;
    cin>>n;
    int a[n];
    int ans=a[0];
    for(int i=0;i<n;i++){
        cin>>a[i];
        if(ans>a[i]) ans=a[i];  
    }
    cout<<ans;

    return 0;
}

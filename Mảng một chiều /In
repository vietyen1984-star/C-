#include <iostream>
#include <vector>
using namespace std;
vector <int> a;
int n;
int main() 
{
    cin>>n;
    a.resize(n);
    for(int i=0;i<n;i++)
    cin>>a[i];
    a.resize(n/2);
    for(int i=0;i<a.size();i++)
    cout<<a[i]<<" ";
    return 0;
}

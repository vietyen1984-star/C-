// in bé-lớn
#include <bits/stdc++.h>
using namespace std;

int main() 
{
    vector <int> a;
    int n;
    cin>>n;
    for(int i=0;i<n;i++){
        int x;
        cin>>x;
        a.push_back(x);
    }
    sort(a.begin(),a.end());
    for(int i=0;i<a.size();i++)
    cout<<a[i]<<" ";
    return 0;
}
// in lớn-bé
#include <bits/stdc++.h>
using namespace std;
bool cmp(int x,int y){
  return x>y;
}
int main() 
{
  vector <int> a;
  int n;
  cin>>n;
  a.resize(n);
  for(int i=0;i<n;i++)
  cin>>a[i];
  sort(a.begin(),a.end(),cmp);
  for(int i=0;i<n;i++)
  cout<<a[i]<<" ";
    return 0;
}
//thêm ô
#include <iostream>
#include <vector>
using namespace std;

int main() 
{
    vector <int> a;
    int n;
    cin>>n;
    for(int i=0;i<n;i++){
        int x;
        cin>>x;
        a.push_back(x);
    }
     for(int i=0;i<a.size();i++)
     cout<<a[i]<<" ";
   

    return 0;
}
//xoá
#include <bits/stdc++.h>
using namespace std;
int main() 
{
       vector <int> a;
    int n;
    cin>>n;
    for(int i=0;i<n;i++){
        int x;
        cin>>x;
        a.push_back(x);
    }
     a.clear();
     cout<<a.size();
    return 0;
}
//nhân chia
#include <bits/stdc++.h>
using namespace std;
int main() 
{
       vector <int> a;
    int n;
    cin>>n;
    a.resize(n);
    for(int i=0;i<n;i++)
    cin>>a[i];
    a.resize(n/2); //a.resize(n*2) 
    for(int i=0;i<a.size();i++) //  for(auto x:a)
     cout<<a[i]<<" ";         
    return 0;
}
